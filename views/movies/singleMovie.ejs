<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('../favicon') %>
    <title><%= movie.title %></title>
  </head>
  <body>
    <%- include('../nav') %>
    <h1><%= movie.title %></h1>
    <ul>
        <li>Year released: <%= movie.year %></li>
        <li>
          Genre:
          <a href="/genres/<%= movie.genre_1_path %>"><%= movie.genre_1 _%></a><%= movie.genre_2 ? ',' : '' %>
          <% if (movie.genre_2) { %>
            <a href="/genres/<%= movie.genre_2_path %>"><%= movie.genre_2 %></a>
          <% } %>
        </li>
        <li>Studio:
          <a href="/studios/<%= movie.studio_path %>"><%= movie.studio %></a>
        </li>
        <li>Number in stock: <%= movie.stock %></li>
    </ul>
    <a href="/movies">&lt;&lt; All movies</a>
  </body>
</html>
